Semantic Vega Editor v1.37

Fixes a runtime failure introduced in v1.36 where the legend-gradient detection used an invalid CSS selector containing url(#...). The detector now scans paint-bearing elements with [fill], [stroke] and tests gradient references in JavaScript, so Run no longer breaks while preserving portable legend-gradient handling.
